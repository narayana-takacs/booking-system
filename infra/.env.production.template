# Production Environment Configuration
# Copy to infra/.env and fill in actual values
# DO NOT commit this file with real values!

# ============================================================================
# n8n Configuration
# ============================================================================

# Encryption key for credentials vault (REQUIRED - use generated value)
N8N_ENCRYPTION_KEY=REPLACE_WITH_GENERATED_KEY

# Basic auth for n8n UI access
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=REPLACE_WITH_GENERATED_PASSWORD

# Production domain configuration
# Example: booking-automation.yourdomain.com
N8N_HOST=REPLACE_WITH_YOUR_DOMAIN

# Webhook base URL (must match N8N_HOST with https://)
# Example: https://booking-automation.yourdomain.com/
WEBHOOK_URL=https://REPLACE_WITH_YOUR_DOMAIN/

# Timezone for workflow execution
GENERIC_TIMEZONE=America/New_York

# ============================================================================
# Database Configuration
# ============================================================================

DB_TYPE=postgresdb
POSTGRES_PASSWORD=REPLACE_WITH_GENERATED_PASSWORD
POSTGRES_USER=n8n
POSTGRES_DB=n8n

# ============================================================================
# Airtable Configuration
# ============================================================================

# Personal Access Token from https://airtable.com/create/tokens
AIRTABLE_PAT=patXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Base ID (found in Airtable API docs or URL: https://airtable.com/appXXXXXXXXXXXXXX)
AIRTABLE_BASE_ID=appXXXXXXXXXXXXXX

# Table names (must match exactly in Airtable)
AIRTABLE_TABLE_AVAILABILITY=Availability
AIRTABLE_TABLE_BOOKINGS=Bookings
AIRTABLE_TABLE_CLIENTS=Clients

# Set to false for production (true = stub mode for testing)
AIRTABLE_USE_STUB=false

# ============================================================================
# Email Configuration (SMTP)
# ============================================================================

# Option 1: SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASSWORD=SG.XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Option 2: AWS SES (us-east-1)
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=AKIAXXXXXXXXXXXXXXXX
# SMTP_PASSWORD=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Option 3: Mailgun
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USER=postmaster@yourdomain.com
# SMTP_PASSWORD=XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Option 4: Generic SMTP
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASSWORD=your-smtp-password

# Provider alert email address
PROVIDER_ALERT_EMAIL=provider@yourdomain.com

# ============================================================================
# Optional: Webhook Security
# ============================================================================

# Uncomment and use generated API key for webhook validation
# WEBHOOK_API_KEY=REPLACE_WITH_GENERATED_API_KEY
